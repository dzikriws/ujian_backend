model master_user {
    id         String   @id @default(uuid())
    name       String   @unique
    username   String   @unique
    password   String
    created_at DateTime @default(now())
    updated_at DateTime @default(now()) @updatedAt

    suplier master_supplier[]
    buyer transaction[]
}
