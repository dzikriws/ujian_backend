model master_user {
    id            Int      @id @default(autoincrement())
    username      String   
    email         String  
    hash_password String
    ts_insert     DateTime @default(now())
    status        String
}
