model master_user {
  username      String
  status        String
  id            Int      @id @default(autoincrement())
  email         String
  hash_password String
  ts_insert     DateTime @default(now())
}
